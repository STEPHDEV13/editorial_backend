PORT=4000
NODE_ENV=development

# Frontend URL (used in article links inside emails)
FRONTEND_URL=http://localhost:3000

# ── SMTP (Nodemailer) ────────────────────────────────────────────────────────
# Gmail: host=smtp.gmail.com  port=587  secure=false
# Use an App Password if 2FA is enabled on your Google account
SMTP_HOST=c4m.mg
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=notification_taram@c4m.mg
SMTP_PASS=7zTsHVKZyC6runc
SMTP_FROM="Editorial TARAM" <notification_taram@c4m.mg>
# Set to false when SMTP_HOST is an IP address (cert cannot match an IP)
SMTP_TLS_REJECT_UNAUTHORIZED=false
